module SwiftMessage.Model.MT535 where

import SwiftMessage.Util (ReportDetail, ReportDetails(..))

-- Same as MT940 but for security deposit
data MT535Details = MT535Details with
    reportDetails: ReportDetails
  deriving (Eq, Show)
 
createMT535Details : [ReportDetail] -> Date -> MT535Details
createMT535Details reportDetails reportDate = 
  MT535Details with reportDetails = ReportDetails with ..
  